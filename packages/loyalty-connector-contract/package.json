{
  "name": "@24mx/loyalty-connector-contract",
  "version": "1.0.0",
  "description": "Request/response schemas, error keys and examples of the Pierce loyalty gift card connector's storefront-facing routes (/balance, /redeem, /finalize, /release)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "examples",
    "README.md"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "jest",
    "lint": "prettier --check \"**/*.{ts,js,json}\"",
    "prepublishOnly": "npm test && npm run build"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@sinclair/typebox": "0.34.52"
  },
  "devDependencies": {
    "@jest/globals": "30.4.1",
    "@types/node": "24.13.3",
    "jest": "30.4.2",
    "prettier": "3.9.6",
    "ts-jest": "29.4.12",
    "typescript": "6.0.3"
  }
}
